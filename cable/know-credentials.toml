[metadata]
name = "know-credentials"
description = "Browse stored credentials"

[source]
command = "know list credentials --format television"

[preview]
command = "know list credentials --format television-preview --entry '{}'"
