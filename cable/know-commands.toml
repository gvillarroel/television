[metadata]
name = "know-commands"
description = "Browse all available sync/export/delete commands"

[source]
command = "know browse commands --format television"
ansi = true

[preview]
command = "know browse commands --format television-preview --entry '{}'"
