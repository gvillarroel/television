[metadata]
name = "know-unsynced"
description = "Sources registered but never synced"

[source]
command = "know browse unsynced --format television"
ansi = true

[preview]
command = "know browse unsynced --format television-preview --entry '{}'"
